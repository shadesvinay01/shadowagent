{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "shell:default",
    "os:default",
    "store:allow-save",
    "store:allow-set",
    "store:allow-get",
    "store:allow-load"
  ]
}
